programa {
    entero a = 3;
    entero b = 7;
    decimal promedio = 8.75;
    entero contador = 1;

    si (a < b) {
        imprimir(a);
    }

    mientras (contador < 6) {
        imprimir(contador);
        contador = contador + 1;
    }

    para (contador = 1; contador < 4; contador = contador + 1) {
        imprimir(contador);
    }
}